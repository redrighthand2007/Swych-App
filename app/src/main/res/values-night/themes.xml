<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="Theme.Swych" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:windowBackground">@color/splash_bg</item>
    </style>

    <style name="Theme.App.Starting" parent="Theme.SplashScreen">
        <!-- Dark theme: black background -->
        <item name="windowSplashScreenBackground">@android:color/black</item>
        <!-- Same vector logo — no cuts, no jagged edges, adapts to bg color -->
        <item name="windowSplashScreenAnimatedIcon">@drawable/ic_swych_logo_fg</item>
        <item name="postSplashScreenTheme">@style/Theme.Swych</item>
    </style>

</resources>
